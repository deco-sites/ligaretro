import Component from "$store/components/product/CustomizeShirt.tsx";
import type { Props } from "$store/components/product/CustomizeShirt.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
