import Component from "$store/components/ui/Form.tsx";
import type { Props } from "$store/components/ui/Form.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
