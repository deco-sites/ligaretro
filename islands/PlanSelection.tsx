import Component from "$store/components/content/PlanSelection.tsx";
import type { Props } from "$store/components/content/PlanSelection.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
